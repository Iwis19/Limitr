spring:
  config:
    activate:
      on-profile: h2
  datasource:
    url: jdbc:h2:mem:limitr;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

app:
  jwt:
    secret: limitr-local-secret-key-change-this-in-production-1234567890
    allow-insecure-secret: true
  seed:
    enabled: true
